query Input {
  cart {
    deliveryGroups {
      id
    }
    cost {
      subtotalAmount {
        amount
      }
    }
  }
  discount {
    discountClasses
    metafield(namespace: "tiered", key: "config") {
      value
    }
  }
}
